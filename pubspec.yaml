name: drinks
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  build_runner: ^2.4.11
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0

dev_dependencies:
  # flutter_test:
  #   sdk: flutter
  flutter_lints: ^3.0.0
  # analyzer: ^4.7.0 
  hive_generator: ^1.1.1



flutter:
  uses-material-design: true

  assets:
    - assets/png/
